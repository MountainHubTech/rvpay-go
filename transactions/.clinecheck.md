# Transactions PawaPay Integration Checklist

- [x] Read PawaPay context and service context
- [x] Inspect legacy Deposits PawaPay wiring
- [x] Inspect current PawaPay client API
- [x] Add Transactions PawaPay configuration
- [x] Wire PawaPay client into Transactions bootstrap
- [x] Integrate available PawaPay deposit methods
- [x] Integrate available PawaPay payout methods
- [x] Add deposit tests
- [x] Add payout tests
- [x] Add configuration/wiring tests
- [x] Run focused tests
- [x] Run vet and formatting checks
- [x] Review scope for unrelated changes
- [x] Append Transactions README
- [x] Final verification complete

## 2026-09-02 — External (HighLevel) deposit identifiers
- [x] Proto: CreateDepositRequest + Deposit `client_id` → `client_name` (field numbers unchanged); regenerated grpc/go via protobuf/Makefile
- [x] Migration 000003 (up/down): deposits client_id UUID→client_name TEXT NOT NULL; customer_id/merchant_id UUID NOT NULL→TEXT NULL; FKs to customers/merchants dropped; indexes rebuilt
- [x] sqlc regenerated via `go generate ./...` (project-pinned versions); Deposit/CreateDepositParams now string-based
- [x] deposits/service.go: uuid.Parse removed; client_name non-empty validation; customer_id/merchant_id optional strings persisted verbatim; UUID customer lookup removed (tenant boundary = client_name); PawaPay flow untouched
- [x] DepositRepo.Create/List* signatures string-based; converters map external strings; NewDepositService no longer takes customerRepo (main.go updated)
- [x] Tests updated: HighLevel-style ids (highlevel-abc123 / ghl-contact-123 / ghl-transaction-456) accepted & forwarded verbatim; missing/blank client_name rejected; duplicate case; no UUID parsing of the three fields remains in the deposit path
- [x] go build ./... (repo root) PASS; go vet ./transactions/... PASS; go test ./transactions/... PASS; gofmt clean on changed files
- [ ] Migration not executed against a live DB; down migration only valid while all stored ids are UUIDs; end-to-end GHL→AWS flow unverified (needs live deployment)

## 2026-09-02 — Nullable text scan fix (external_reference NULL)
- [x] Root cause: sqlc.yaml override forced nullable text → plain `string` (cannot scan SQL NULL); removed the `nullable: true` text override
- [x] sqlc regenerated (`go generate ./db`, pinned v1.29.0): nullable text now `*string` (emit_pointers_for_null_types); timestamps already pgtype.Timestamptz
- [x] deposit_repo/payout_repo adapted via textPtr (""→NULL for absent external ids) / textRef (exact value pass-through); mocks regenerated
- [x] deposit/payout converters deref NULL → "" at wire layer only; NULL vs "" preserved in persistence
- [x] New tests converters_test.go: Case1 all-NULL row, Case2 external ids only, Case3 populated values
- [x] go build ./... (root) PASS; go vet ./transactions/... PASS; go test ./transactions/... PASS
- [x] No new migration (schema already nullable; version 3 clean); proto/API contract and admindashboard/page.tsx unchanged

## 2026-09-02 — VerifyPayment GET contract + transactional InitiateDeposit
- [x] proto: VerifyPayment POST → GET /v1/public/payments/verify; query params ghlTransactionId/ghlChargeId/subscriptionId bind to unchanged VerifyPaymentRequest fields; grpc-gateway regenerated (mux.Handle("GET", pattern_PaymentService_VerifyPayment_0) confirmed)
- [x] admindashboard polling already GET — unchanged
- [x] InitiateDeposit now: BEGIN (TransactionsRepo.Begin, existing mechanism) → INSERT via repo.NewDepositRepo(txQuerier) → PawaPay initiation → COMMIT; any failure → deferred ROLLBACK; commit failure → Internal
- [x] VerifyPayment business semantics unchanged; external identifier model & NULL handling (nullable text = *string) unchanged
- [x] New tests: Begin failure, create failure rollback, duplicate rollback, PawaPay failure rollback (no commit), commit failure, happy-path commit, gateway GET query-param binding test
- [x] go build ./... (root) PASS; go vet ./transactions/... PASS; go test -count=1 ./transactions/... PASS
- [ ] Live AWS/HighLevel/PawaPay verification pending deployment (501 fix + rollback behavior in production unverified)

## 2026-09-02 — Corrected HighLevel transactionId mapping
- [x] proto: CreateDepositRequest + `string ghl_transaction_id = 8` (new field number); grpc-gateway regenerated
- [x] deposits.sql CreateDeposit INSERT now persists ghl_transaction_id into the existing 000002 column; sqlc + mocks regenerated; no new migration
- [x] DepositRepo.Create gained ghlTransactionID param (empty → SQL NULL via textPtr); service passes req.GetGhlTransactionId() verbatim; no UUID parsing/fabrication
- [x] merchant_id now receives the payer phone number (temporary/current requirement) — never the transactionId
- [x] VerifyPayment unchanged (GET contract kept) and resolves via deposits.ghl_transaction_id → ghl_charge_id fallback; ghl_charge_id correlation preserved
- [x] Tests: exact mapping asserted (merchant_id=+237654131027 ≠ ghl_transaction_id=6a981bf9111e4879c418ffee, client_name=highlevel-…, customer_id=contact.id); all tx/rollback tests preserved
- [x] go build ./... (root) PASS; go vet ./transactions/... PASS; go test -count=1 ./transactions/... PASS
- [ ] Live GHL→AWS→PostgreSQL verification pending deployment

## 2026-09-02 — PawaPay initiation status handling
- [x] initiatePawapayDeposit captures the response and switches on Status: ACCEPTED → nil (tx commits); REJECTED → structured failure log (deposit_id, failure_code, failure_message, provider, phone_number, amount, currency; no secrets) + error (tx rolls back); unexpected status → error (tx rolls back); transport error → error (tx rolls back)
- [x] DB transaction untouched (Begin → INSERT → PawaPay → COMMIT, deferred rollback); REJECTED/unexpected/transport failures never commit
- [x] Phone number verified: '+' is trimmed via strings.TrimPrefix before building the SDK request → serialized JSON contains MSISDN 237600000000 (no '+'); removed stray fmt.Println debug; happy-path test asserts the exact serialized phoneNumber
- [x] New tests: REJECTED (HTTP 200, INVALID_AMOUNT) → rollback/no commit; UNKNOWN_STATUS → rollback/no commit; 500 transport → rollback/no commit (existing); ACCEPTED → commit (existing, now also asserts MSISDN format)
- [x] go build ./... (root) PASS; go vet ./transactions/... PASS; go test -count=1 ./transactions/... PASS; gofmt clean on changed files
- [ ] Live PawaPay verification pending; no callbacks implemented (out of scope per task)

## 2026-09-02 — PawaPay zero-decimal XAF amount serialization
- [x] pawapayAmountString(): XAF is zero-decimal on PawaPay — exact integrality check on the numeric mantissa (math/big, no float rounding); fractional XAF amounts rejected before the PawaPay call; whole numbers serialized without decimals (25.00 → "25")
- [x] Non-XAF currencies keep the existing two-decimal serialization
- [x] DB transaction, provider mapping, phone normalization, SDK usage unchanged; rejection rolls back via the existing deferred rollback and never calls PawaPay
- [x] Tests: 25.00→"25", 1000.00→"1000", 25.50/25.05 rejected, USD keeps 2 decimals; end-to-end 25.00 XAF asserts serialized body amount=="25"; 25.50 XAF asserts PawaPay not called + rollback
- [x] go build ./... (root) PASS; go vet ./transactions/... PASS; go test -count=1 ./transactions/... PASS; gofmt clean on changed files
- [ ] Live PawaPay INVALID_AMOUNT confirmation pending deployment

## 2026-09-02 — PawaPay V2 Deposit Status Callback
- [x] proto: PaymentService.ProcessDepositCallback → POST /v1/public/pawapay/deposits/callback (new messages, no field renumbering); gateway regenerated, route verified registered as POST
- [x] Gateway DiscardUnknown confirmed → extra PawaPay fields (payer, metadata, created) safely ignored; nested failureReason mapped
- [x] Business logic in payments.Impl: correlation strictly by deposit_id (RVPay deposit UUID); COMPLETED→MarkCompleted, FAILED→MarkFailed(failure_message), PROCESSING→UpdateStatus; providerTransactionId → external_reference; never touches ghl_transaction_id
- [x] Idempotency: duplicate terminal callbacks acknowledged 200 with zero mutations; terminal-state protection ignores conflicting downgrades (logged, 200)
- [x] Unknown depositId → 200 ack (no pointless retries); unsupported status / missing fields → InvalidArgument (400); repo lookup error → Internal (retryable)
- [x] New SQL UpdateDepositExternalReference (:exec) + DepositRepo.SetExternalReference; sqlc + mocks regenerated; NO new migration (external_reference/status columns already exist)
- [x] Tests: payments/callback_test.go (validation, COMPLETED/FAILED/PROCESSING, idempotent duplicates, terminal protection, unknown deposit, lookup error, GHL-reference isolation) + gateway POST binding test
- [x] go build ./... (root) PASS; go vet ./transactions/... PASS; go test -count=1 ./transactions/... PASS; gofmt clean on changed Go files
- [ ] Signature verification NOT implemented: signed callbacks not confirmed enabled for the account; seam is the gateway boundary — see service-context decision
- [ ] Live PawaPay callback delivery unverified; requires callback URL configured in PawaPay Dashboard + public HTTPS routing

## 2026-09-04 — Server-Connection Mapping (read-only)
- [x] Dashboard POST /v1/public/deposits + GET /v1/public/payments/verify verified against proto contracts — MATCH, no change needed
- [x] Overview snapshot + payouts list endpoints: page still static — documented as delegated tasks only; no changes made here

## 2026-09-04 — Customer transactional flow + client_name migration
- [x] Migration 000004_customer_client_name (up/down): customers client_id UUID→client_name TEXT; merchant_id nullable; +name, +address; unique(client_name, phone_number); NO FK to clients (client_name not unique)
- [x] customers.sql rewritten (client_name, GetCustomerByClientNameAndPhone, ON CONFLICT DO NOTHING)
- [x] sqlc.yaml: removed nullable-uuid override (pgtype.UUID scans NULL); regenerated sqlc + mocks
- [x] proto: Customer{client_name,name,address}, CreateCustomerRequest{client_name,name,address}, CreateDepositRequest.customer=9; regenerated grpc/go
- [x] customer_repo.go: client_name-based API, uuidToPg helper for nullable merchant
- [x] customers service/converters: client_name, optional merchant, name/address, deref at wire layer
- [x] deposits service: customer resolve-or-create inside same tx as deposit+PawaPay; NewDepositService takes customerRepo; main.go wired
- [x] 9 rollback/reuse tests (service_customer_test.go) + updated existing tests; go test ./transactions/... PASS; gofmt clean

## 2026-09-04 — Dashboard data endpoints (overview + payouts)
- [x] proto: PayoutService.GetPayoutOverviewStats → GET /v1/public/payouts/overview/stats; PayoutService.ListPayouts → GET /v1/public/payouts; DashboardOverviewService.GetOverviewSnapshot → GET /v1/public/overview/snapshot (v1/public convention)
- [x] SQL: deposit aggregates (SumDepositAmountInWindow, CountDepositsInWindow, RevenueOverTimeInWindow COALESCE, ListRecentDeposits); payout aggregates + ListPayoutsFiltered/CountPayoutsFiltered (search/status/pagination); sqlc regenerated
- [x] payout service: GetPayoutOverviewStats (pending=REQUESTED+PROCESSING, cleared, failed), ListPayouts (initials/money/status mapping, Pending|Cleared|Failed)
- [x] NEW transactions/overview package: GetOverviewSnapshot (revenue, volume, revenue-over-time buckets, recent payouts); registered in main.go grpc + gateway
- [x] Cross-service gaps NOT fabricated: activeSubAccounts=0 + needsAttention empty (Clients-owned/disputes absent), documented in proto comments + dashboard-setup.md
- [x] go build/vet/test ./transactions/... PASS; gofmt clean on changed files

## 2026-09-04 — Migration 000004 local-run failure fix
- [x] Failure: pq: relation "idx_customers_merchant_id" already exists (created in 000001; survives the client_id rename + merchant_id nullability change)
- [x] Fix: 000004 up now uses CREATE INDEX IF NOT EXISTS for idx_customers_merchant_id (smallest change; intended schema fully preserved)
- [x] Local DB was left dirty=true/version=4 by the failed run; user manually reset to 3/false — verified the DDL had fully rolled back (clean pre-000004 schema, 0 rows) before re-running
- [x] Verified: schema_migrations=4/dirty=false; client_name TEXT, name, address, merchant_id nullable, idx_customers_client_name, idx_customers_merchant_id (once), uq_customer_client_name_phone
- [x] make run: "Migrations applied successfully" + gRPC :50052 + gateway :8080 startup; go test ./transactions/... PASS; go vet clean; no data changed manually

## 2026-09-06 — Server observability: dashboard overview + payout endpoints instrumented
- [x] GetOverviewSnapshot: entry (period) / per-repo results (SumAmountInWindow, CountInWindow, RevenueOverTimeInWindow, PayoutRepo.ListFiltered; zero-row INFO visibility) / completion (total_revenue, currency, transaction_volume, revenue_points_count, recent_payouts_count, duration) / failure (grpc_code, duration) logs
- [x] GetPayoutOverviewStats: entry / repo counts+sums results / completion (stats_count, duration) / failure logs
- [x] ListPayouts: entry (search/status/page/page_size) / repo rows_returned + zero-row INFO / completion (rows_returned, total, page, page_size, duration) / failure logs
- [x] No contract/SQL/pagination/PawaPay change; payment-page endpoints untouched
- [x] go build, go vet, go test ./transactions/... PASS; gofmt: pre-existing repo-wide CRLF, not reformatted

## 2026-09-06 — CORS moved to shared infrastructure + localhost dashboard origin
- [x] transactions/cmd/grpc-service local corsMiddleware/parseAllowedOrigins moved to shared/observability.CORS/ParseAllowedOrigins (identical semantics) so Clients reuses the same policy; gateway_test.go updated to the shared middleware
- [x] CORSAllowedOrigins default now includes http://localhost:3000 alongside https://admindashboard.rvpay.xyz (env HTTP_CORS_ALLOWED_ORIGINS to override)
- [x] Root cause of dashboard "Failed to fetch": local/deployed dashboard origins were not on the allowlist (and Clients had no CORS at all); services themselves returned 200
- [x] Tests: shared cors_test.go (7 scenarios) + existing gateway tests PASS; go build/vet/test ./transactions/... PASS

## 2026-09-07 — Runtime CORS root cause + resolution fix
- [x] Live curl diagnosis: middleware ACTIVE (deployed origin received headers) but runtime allowlist lacked http://localhost:3000 (stale HTTP_CORS_ALLOWED_ORIGINS overriding the new default); preflights from localhost:3000 fell through to gateway → 501
- [x] Fix: ResolveAllowedOrigins (local dev origin always ensured; empty config → defaults), CORS decision logging, startup allowlist log — wired into main.go
- [x] Regression tests: TestResolveAllowedOrigins_* added; full go test/vet/build pass
- [x] Live proof against current-build instance (:18081): all curl scenarios pass (see clients checkpoint for symmetric details)

## 2026-09-06 — Overview snapshot route relocated under /v1/public/transactions*
- [x] GET /v1/public/overview/snapshot → /v1/public/transactions/overview/snapshot (GetOverviewSnapshot RPC unchanged)
- [x] transactions.proto HTTP annotation updated; gateway regenerated; old route no longer registered
- [x] overview service logging endpoint label updated to the real route; gateway tests: OverviewSnapshotRoute (new path reaches RPC) + NotOnOldPath (old path not 200)
- [x] CORS unaffected (shared root middleware, route-independent); go build/vet/test PASS

## 2026-09-08 — Minimal admin authentication: dashboard endpoints protected (APPENDED)
- [x] transactions/auth/middleware.go NEW: delegates access-token validation to the Clients service over the internal AuthService.ValidateAccessToken gRPC (CLIENTS_GRPC_ADDR) — no duplication of users/access_tokens state
- [x] Admin middleware (shared semantics with clients/auth) protects the three dashboard read endpoints at the transport layer: GET /v1/public/transactions/overview/snapshot, GET /v1/public/payouts/overview/stats, GET /v1/public/payouts
- [x] ALB constraint: listener rules only permit /v1/public* prefixes; /v1/admin is NOT routable → endpoints RETAIN their permitted prefixes with logical protection (§21 compliant alternative); ALB YAML untouched
- [x] PUBLIC exemptions preserved: POST /v1/public/deposits (InitiateDeposit), GET /v1/public/payments/verify, all deposit/payout/refund/checkout callbacks, payments webhook, healthcheck — never intercepted
- [x] Authorization header / tokens never logged; 401 with WWW-Authenticate Bearer on rejection
- [x] Config: CLIENTS_GRPC_ADDR added to transactions/config/model.go; .env.example documented
- [x] go build/vet/test ./transactions/... PASS; no proto contract, SQL, migration, or PawaPay changes

## 2026-09-08 — Correction: database-managed administrators (APPENDED)
- [x] No Transactions changes required: middleware already delegates to Clients ValidateAccessToken, which checks user_role='USER_ROLE_ADMIN' via the SQL join (verified by new clients/auth tests incl. token isolation + expiry)
- [x] CLIENTS_GRPC_ADDR / ACCESS_TOKEN_TTL / REFRESH_TOKEN_TTL remain the only auth-related env; ADMIN_* removed repo-wide
- [x] Route protection, ALB-compatible /v1/public prefixes, payment-page exemptions: unchanged
## 2026-09-08 — RVPay module-path migration to MountainHubTech org (APPENDED)
- [x] **Old path:** `github.com/I-Frostbyte/rvpay-go` → **New path:** `github.com/MountainHubTech/rvpay-go`, migrated repo-wide (go.mod module, all Go imports incl. `transactions/*` and `clients/auth`, protobuf `go_package`, regenerated grpc/go + `*/db/{sqlc,repo}/mocks`, render.yaml, README, deploy docs).
- [x] PawaPay SDK (`github.com/I-Frostbyte/pawapay_client` + `/deposits`,`/payouts`) EXEMPT and untouched — external dependency, remains pinned under the personal account.
- [x] Transactions Docker build fix: `transactions/auth/middleware.go` imports repo-local `clients/auth`; added `COPY clients ./clients` to `transactions/Dockerfile` so the repo-local package resolves in the build context (no old-repo download, no auth DB duplication).
- [x] Verification: `go build ./...` PASS; `go vet ./...` PASS; `go test ./transactions/... ./clients/... ./shared/...` all ok; `go test ./...` no failures; go.sum unchanged (0 RVPay self-references).
## 2026-09-10 — Handoff: ListTransactions backend IMPLEMENTED (APPENDED)
- [x] protobuf/transactions.proto: DashboardOverviewService.Added ListTransactions RPC → GET /v1/public/transactions (+ ListTransactionsRequest/Row/Response); generated grpc/go transactions pb + gateway files regenerated
- [x] db/query/deposits.sql: Added ListDepositsFiltered + CountDepositsFiltered (search/status/sub_account; LIMIT/OFFSET); sqlc regenerated (deposits.sql.go + querier.go) + Querier + repo mocks regenerated
- [x] db/repo/deposit_repo.go: DepositRepo interface + impl gained ListFiltered(ctx, search, status, subAccount, limit, offset) and CountFiltered(...)
- [x] overview/service.go: Impl.ListTransactions implements the RPC (pagination defaults 1/20/max100; maps deposit→TransactionListRow: shortId, subAccount=client_name, customer, initials, amount, status Success|Failed|Pending, gateway MTN/Orange MoMo, date); observability logs added
- [x] cmd/grpc-service/main.go: /v1/public/transactions added to admin protectedRoutes list
- [x] Verification: go build ./... PASS; go vet clean; go test ./transactions/... ./clients/... PASS
## 2026-09-10 — COMPLETION: Disputes backend implemented (APPENDED)
- [x] db/migrations/000005_disputes.{up,down}.sql NEW: disputes table (dispute_status enum NEEDS_RESPONSE/UNDER_REVIEW/RESOLVED; deposit FK; evidence_submitted; opened/due/resolved timestamps) + indexes
- [x] db/query/deposits.sql: +ListDisputesFiltered, CountDisputesFiltered, GetDisputeStats, GetDisputeByID, InsertDispute, SubmitEvidence; sqlc regenerated (deposits.sql.go, querier.go) + Querier + repo mocks regenerated
- [x] db/repo/dispute_repo.go NEW: DisputeRepo (ListFiltered/CountFiltered/GetStats/GetByID/SubmitEvidence)
- [x] protobuf/transactions.proto: DashboardOverviewService + GetDisputeStats (GET /v1/public/transactions/disputes/stats), ListDisputes (GET .../disputes), SubmitEvidence (POST .../disputes/evidence, static path so transport middleware matches exactly) + messages; generated grpc/go transactions files regenerated
- [x] overview/service.go: Impl.GetDisputeStats / ListDisputes / SubmitEvidence + helpers (disputeAmount, disputeStatusLabel, dueInLabel); Impl now takes DisputeRepo
- [x] cmd/grpc-service/main.go: dispute routes added to protectedRoutes; disputeRepo wired into overview service
- [x] Verification: go build ./... PASS; go vet clean; go test ./transactions/... ./clients/... PASS

## 2026-09-11 — GHL v3 Payment-Status Synchronization (APPENDED)

- [x] Migration `000006_ghl_order_sync` (up/down): adds `ghl_order_id`, `ghl_sync_status`, `ghl_sync_attempts` to `deposits` + index.
- [x] `transactions/db/query/deposits.sql`: `FinalizeDepositAndQueueGhlSync`, `ClaimPendingGhlSync`, `MarkGhlSyncRetry`, `MarkGhlSyncFailure`, `MarkGhlSyncSuccess`, `GetDepositByGHLOrderID`.
- [x] `transactions/db/sqlc/{deposits.sql.go,querier.go,models.go}` regenerated; `transactions/db/sqlc/mocks/querier.go` regenerated.
- [x] `transactions/db/repo/deposit_repo.go`: DepositRepo interface + impl extended with new methods; `transactions/db/repo/mocks/repo.go` regenerated.
- [x] `transactions/deposits/service.go`: persists `ghl_order_id` on deposit creation.
- [x] `transactions/payments/service.go`: wraps finalize in repo transaction so deposit terminal status + `ghl_sync_status='pending'` commit together; external GHL call is after-commit.
- [x] `transactions/payments/callback_test.go` REWRITTEN/completed: validation, COMPLETED/FAILED/PROCESSING callbacks, terminal protection, unknown deposit, lookup errors.
- [x] `transactions/ghlsync/worker.go` NEW: outbox worker that claims pending sync rows and dispatches to Clients via internal gRPC.
- [x] `transactions/ghlsync/worker_test.go` NEW: 16 tests covering success/failure/retry/completion/malformed IDs/claim guards/generated-code consistency.
- [x] `transactions/cmd/grpc-service/main.go`: GHL sync worker started.
- [x] Proto: `protobuf/transactions.proto` extended with `ghl_order_id` in deposit creation; `grpc/go/transactionsgrpc/*.pb.go` regenerated.
- [x] Verification: go build ./transactions/... ./clients/... PASS; go vet ./transactions/... ./clients/... PASS; go test ./transactions/... ./clients/... PASS; gofmt clean on changed packages.
- [ ] LIVE verification against a real GHL deployment is not possible in this environment (documented follow-up).

## 2026-09-11 — GHL order payment sync fix: record-payment endpoint (APPENDED)
- [x] `transactions/ghlsync/worker.go`: extracts real deposit amount (pgtype.Numeric → minor units/cents) and passes it as `Amount` on `UpdateGhlOrderStatusRequest`; PawaPay stays authoritative; two-try retry/failure behavior unchanged.
- [x] `clients.proto` (`UpdateGhlOrderStatusRequest`) + regen: `int64 amount = 4`; `grpc/go/clientsgrpc/clients.pb.go` regenerated via protobuf/Makefile.
- [x] `transactions/ghlsync/worker_test.go`: `testDeposit` seeds an amount; `TestWorker_PassesAmountFromDeposit` asserts 150.50 → 15050 cents flows through; generated-code consistency test updated.
- [x] go build ./... PASS; go vet ./transactions/... ./clients/... PASS; go test ./transactions/... ./clients/... PASS; gofmt clean on changed packages.
- [ ] LIVE verification of the GHL order payment record response/resulting order status requires a live deployment.
