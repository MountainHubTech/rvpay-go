# .clinecheck — Agent 02 task plan (HighLevel Marketplace OAuth Fix)

## 1. INSTALL establishes RVPay tenancy
- [x] Resolve HighLevel platform via `PlatformRepo.GetBySlug("highlevel")`; NEVER create.
- [x] Resolve/reuse client for the GHL sub-account; create when missing.
- [x] Resolve/reuse integration (`client_id` + platform); create when missing with
      `external_account_id = locationId`.
- [x] Make INSTALL idempotent (repeated INSTALL reuses existing records).
- [x] Ensure `webhooks/service.go` INSTALL path provisions client+integration BEFORE the
      `webhook_events` insert (FK `integration_id` dependency).
- [x] Wire `clientsRepo` into `webhooks.NewService` (INSTALL provisioning uses existing
      platform repo; dispatcher constructor unchanged — it already resolves by
      `external_account_id`/config).
- [x] Still create/find `payment_provider_configs` idempotently (unchanged intent).

## 2. OAuth callback resolves integration by locationId
- [x] No-state `HandleCallback` resolves via
      `external_account_id` then `payment_provider_configs.location_id`.
- [x] Pass the already-exchanged token response into downstream processing.

## 3. Exchange code exactly once
- [x] Fixed double exchange: no-state `HandleCallback` exchanges once, then calls
      `processCallbackWithToken` (no re-exchange). `ProcessCallback` also exchanges
      once and calls `processCallbackWithToken`. The raw code is never re-exchanged.
- [x] Keep state-based flow (single exchange in `ProcessCallback`) intact.

## 4. Tests
- [x] INSTALL creates client + integration (`TestProcessWebhook_InstallProvisionsTenantAndConfig`).
- [x] Repeated INSTALL reuses existing records (`TestProcessWebhook_InstallRepeatedReusesRecords`).
- [x] Integration maps to HighLevel `locationId` (asserted in provisioning test).
- [x] Marketplace callback resolves the integration by `locationId` (existing tests + new).
- [x] Authorization code exchanged exactly once (`TestHandleCallbackNoState_ExchangesCodeExactlyOnce`).
- [x] Existing state-based callback behavior remains intact (full oauth suite passes).

## 5. Verification
- [x] `go build ./...` OK, `go vet ./...` clean, `go test ./... -count=1` all packages pass.
- [x] Diff reviewed — only `clients/` INSTALL/OAuth call path files changed; no platform
      creation, no legacy/deposit/pawa changes, no infra/secrets.
- [x] `.clinecheck.md` updated.