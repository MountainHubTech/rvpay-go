# Cline Check — Dockerization Sequence Verification

Lightweight verification checklist for the entire Dockerization sequence. Agents may mark only checks they actually verified.

## Bootstrap

- [x] Bootstrap complete (Agent 00)
- [x] Containerization reconnaissance complete (Agent 01)
- [x] Application configuration valid (Agent 02)
- [x] Docker build valid (Agent 03)
- [x] Production container starts (Agent 03/06)
- [x] Runtime environment handling verified (Agent 04)
- [x] Makefile workflow valid (Agent 05)
- [x] Production image is appropriately minimal (Agent 03/06)
- [x] No secrets copied into the image (Agent 03/04/06)
- [x] Final AWS-readiness assessment complete (Agent 06)

## Payment Checkout (paymentsUrl iframe) — HighLevel flow fix

- [x] Payment page guard fixed: only `transactionId` required (apiKey is server-side)
- [x] Query body sends documented `type: "verify"`
- [x] Amount prefilled from `amount` query param when present
- [x] Syntax check passes (esbuild transform; full tsc/build blocked by sandbox node_modules)

## Payment Checkout — payment_initiate_props postMessage contract

- [x] `ready` event posted to parent on iframe mount (GHL handshake)
- [x] `payment_initiate_props` message listener captures amount/currency/transactionId/orderId/contact
- [x] transactionId now sourced from the event (URL params kept as fallback)
- [x] Amount, currency→country, and customer phone prefilled from the event
- [x] Syntax check passes (esbuild transform)

## Agent 00-payment-page-agent — transactionId authority + real initiation

- [x] payment_initiate_props SUSPENDED (kept as diagnostics/fallback, non-blocking)
- [x] transactionId read authoritatively from the payment URL query string
- [x] Pay button calls the EXISTING `POST /v1/public/deposits` initiation endpoint
- [x] Status polling moved to the EXISTING `GET /v1/public/payments/verify`
- [x] No fabricated IDs; duplicate submissions blocked by isSubmitting
- [x] `[RVPay]` iframe diagnostics and `custom_provider_ready` retained
- [x] Syntax check passes (esbuild; full build still sandbox-blocked)

## Surgical correction — buyNowProductId removal + ungated initiation

- [x] `buyNowProductId` no longer mapped as transactionId (explicit code note)
- [x] No transactionId fabricated; unavailable = null
- [x] `if (!transactionId)` gate removed from handlePay
- [x] Pay reaches existing `POST /v1/public/deposits` regardless of GHL id
- [x] GHL verify polling only when a REAL transactionId exists
- [x] Amount validation added; backend error surfaced honestly
- [x] HTTP 200 ≠ fake success; no fabricated polling path
- [x] `[RVPay]` diagnostics + `custom_provider_ready` preserved
- [x] Syntax check passes (esbuild transform)




## Payment page redo — working /payment/checkout handshake reproduced

- [x] Working implementation protocol inspected and identified (retried, stringified ready; JSON-string message parsing; both props events)
- [x] `message` listener registered before the ready post
- [x] Ready message JSON-stringified with `loaded: true` + `addCardOnFileSupported: false`
- [x] Ready message retried on ~500ms interval; stops on payment-context arrival; cleared on unmount
- [x] Listener safely parses JSON-string AND object `event.data`; malformed messages ignored
- [x] `payment_initiate_props` and `setup_initiate_props` both accepted; real transactionId captured (orderId never substituted)
- [x] transactionId preferred from the event; genuine URL param kept as fallback; never fabricated
- [x] Fail-safe: "Invalid payment request. Missing payment transaction information." when no real ID after both sources
- [x] Existing `POST /v1/public/deposits` initiation and `GET /v1/public/payments/verify` polling unchanged; no invented endpoint/field
- [x] UI, `[RVPay]` diagnostics, duplicate-submission guard, single polling interval preserved
- [x] Syntax check passes (esbuild TSX transform; full tsc/build still environment-blocked)

## 2026-09-02 — HighLevel payment identifier mapping fix
- [x] initiatePayment now sends: clientName=`highlevel-${locationId}` (existing RVPay client-name convention), customerId=`payment_initiate_props.contact.id`, merchantId=`payment_initiate_props.transactionId`
- [x] Fails safely when locationId / contact.id / transactionId are missing; no identifiers fabricated; old clientId/customerId/merchantId URL-param mapping removed
- [x] HighLevel `payment_initiate_props` iframe handshake untouched; payment fields (amount, currency, paymentType, payerPhoneNumber, provider) unchanged
- [x] Matches regenerated CreateDepositRequest contract (client_name=1, customer_id=2, merchant_id=3)
- [ ] FULL tsc/next build could NOT run here (WSL; node_modules incomplete, only Windows Node available) — verified by source inspection only; run `npm run build` in the Windows environment

## 2026-09-02 — VerifyPayment GET contract (backend)
- Backend now serves GET /v1/public/payments/verify?ghlTransactionId=... (POST mapping removed); dashboard polling unchanged, no 501 expected after deploy


## 2026-09-02 — HighLevel transactionId mapping correction
- page.tsx now sends merchantId=<payer phone number> and ghlTransactionId=<genuine HighLevel transactionId>; transactionId no longer mapped to merchantId; handshake and fail-safe behavior unchanged


## 2026-09-02 — PawaPay callback backend
- Backend endpoint added: POST /v1/public/pawapay/deposits/callback (no auth, idempotent, 200 on success/duplicates); dashboard code untouched


## 2026-09-02 — Payment polling semantics fix
- [x] Polling keeps GET /v1/public/payments/verify (queryPaymentStatus unchanged), interval now the PAYMENT_POLL_INTERVAL_MS constant (3000ms)
- [x] Pending semantics fixed: only failed === true is terminal failure; success === false alone no longer stops polling; both false → keep polling
- [x] Temporary fetch/network/server errors: logged via console.warn and polling continues inside the window
- [x] 10-minute timeout via PAYMENT_POLL_TIMEOUT_MS + pollDeadlineRef; timeout stops polling, sets isSubmitting(false), shows the timeout message, never re-initiates
- [x] stopPolling() clears interval + deadline; unmount effect clears both; single active interval guaranteed (stopPolling before starting)
- [x] Duplicate submissions still blocked by isSubmitting while polling; initiation/handshake/identifiers untouched
- [x] Syntax validated (esbuild TSX transform, no errors); full tsc/next build still environment-blocked (broken Linux node_modules, Windows-only Node) — run npm run build on Windows

## 2026-09-02 — Terminal payment-result modal
- [x] PaymentResult state ("completed"|"failed"|null) set only from polling terminal states: success===true → "completed"; failed===true → "failed"; timeout/transport errors do NOT open the modal
- [x] Modal: centered overlay (bg-black/50, z-50), role=dialog + aria-modal + aria-labelledby; green ✓ circle + "Payment completed" / red ✕ circle + "Payment failed"; blue OK button (autofocus, keyboard accessible)
- [x] OK: no HighLevel products/return URL exists anywhere in the current integration (props type, URL params, config, checkout) — no URL fabricated; OK logs a diagnostic and dismisses the modal; documented safe seam for a future legitimate returnTo
- [x] Polling behavior (interval, timeout, pending semantics, error retry), initiation, handshake untouched; only page.tsx changed
- [x] esbuild TSX transform PASS; full tsc/next build still environment-blocked — run npm run build on Windows

## 2026-09-04 — Admin Dashboard Server-Connection Mapping
- [x] Every active fetch mapped: only the two /payment calls (POST /v1/public/deposits, GET /v1/public/payments/verify) touch the network
- [x] Request bodies verified against protobuf/transactions.proto + protobuf/common.proto (client_name/customer_id/merchant_id/ghl_transaction_id/Money/Provider/PaymentType) — MATCH, no dashboard change required
- [x] FCFA→XAF, mtn→PROVIDER_MTN_MOMO, orange→PROVIDER_ORANGE_MOMO mappings verified
- [x] Overview/sub-accounts/payouts/transactions/disputes/settings/me/notifications: static or placeholder — no active requests (classification D), gaps documented in .project-checkpoint.md
- [x] No code changes made in admindashboard/ during this mapping

## 2026-09-04 — Server-Connection Implementation (Customer flow)
- [x] app/payment/page.tsx: sends customer{name, phoneNumber, address} in CreateDepositRequest; shippingAddressToText helper added; no fabricated values
- [x] .dashboard-setup.md §3 rewritten: LIVE vs FUTURE/NOT LIVE classification with full CreateDepositRequest contract

## 2026-09-04 — Server-Connection Implementation (dashboard wiring)
- [x] lib/api.ts NEW: fetch helpers for overview snapshot, payout stats, payout list, sub-accounts (v1/public convention, hardcoded API base like page.tsx)
- [x] / Overview: dashboard-home.tsx fetches GET /v1/public/overview/snapshot per period; no fabricated data (empty/— until live)
- [x] /payouts: page fetches stats + list; PayoutsOverview server-driven (search/status/page)
- [x] /sub-accounts: page fetches list; SubAccountsTable server-driven; gaps render "—"
- [x] .dashboard-setup.md: LIVE table extended (6 endpoints); FUTURE = sub-accounts POST (UI performs no POST), payout export/detail, transactions/disputes/settings, /me
- [x] Full tsc/next build env-blocked (incomplete node_modules, pre-existing); no dangling static-data references

## 2026-09-06 — Surgical TS build correction (payouts/sub-accounts status narrowing + pagination)
- [x] app/payouts/page.tsx: status state typed `PayoutOverviewStatus | "All"`; parsePayoutStatus narrows generic strings against the supported status list; unknown/malformed → "All"
- [x] app/sub-accounts/page.tsx: status state typed `SubAccountStatus | "All"`; parseSubAccountStatus narrows generic strings; unknown/malformed → "All"
- [x] components/dashboard/payouts-overview.tsx: prev/next call setPage with concrete numbers (Math.max(1, page-1) / Math.min(pageCount, page+1)); bounds preserved
- [x] No `any`, no @ts-ignore/@ts-expect-error, no blanket casts; no API/Docker/dependency changes
- [x] npm run build: PASS (TypeScript phase compiled successfully, exit 0)
- [x] npm run lint: 4 pre-existing react-hooks/set-state-in-effect errors only (also in untouched dashboard-home.tsx / payment/page.tsx); none TS-related

## 2026-09-06 — Server-side observability for live Dashboard read endpoints
- [x] GET /v1/public/overview/snapshot, /payouts/overview/stats, /payouts (Transactions) + /sub-accounts (Clients): request-received / repository rows_returned / zero-row / completion / error logs with request_id, params, status, duration
- [x] shared/observability AccessLog now logs the raw query string; existing X-Request-ID correlation reused (no new framework)
- [x] Payment-page endpoints (POST /v1/public/deposits, GET /v1/public/payments/verify) intentionally NOT instrumented
- [x] Dashboard-side verified: lib/api.ts fetches are the only dashboard read calls; no dashboard code changed (observability is server-side)
- [x] go build + go vet + go test ./clients/... ./transactions/... ./shared/... PASS; gofmt: repo-wide pre-existing CRLF (untouched files also flagged), no reformat performed

## 2026-09-06 — Settings environment switcher (Local / Testing / Production)
- [x] lib/environments.ts NEW: centralized config (clientsBaseUrl + transactionsBaseUrl per environment), localStorage key rvpay-dashboard-environment, default testing, SSR-safe runtime resolvers, malformed-value fallback
- [x] lib/api.ts: fetchOverviewSnapshot / fetchPayoutOverviewStats / fetchPayouts → transactionsBaseUrl; fetchSubAccounts → clientsBaseUrl; resolved per request; endpoint paths/contracts unchanged
- [x] app/settings/page.tsx: Environment section — Local/Testing/Production buttons (selected highlighted + aria-pressed), "Current environment:" display, live Clients/Transactions API base URLs; useSyncExternalStore (SSR-safe, no new lint error)
- [x] app/payment/page.tsx: two hardcoded api.rvpay.xyz URLs now resolve from the selected environment's transactionsBaseUrl; payment flow otherwise untouched
- [x] Hardcoded-URL sweep: app/components/lib contain environment URLs ONLY in lib/environments.ts
- [x] npm run build PASS (Compiled successfully, exit 0); npm run lint: 4 pre-existing set-state-in-effect errors only (none in new code)
- [x] Runtime logic verified via tsc-compiled node test: default=testing; local→:8080/:8081; testing→api.rvpay.xyz; production→api.rvpay.co; bogus stored value→testing; per-request resolution means switching needs no rebuild/reload

## 2026-09-06 — Failed-to-fetch end-to-end diagnostics
- [x] lib/api.ts: [RVPay API] request logging (env, service, exact URL/method, status; failures always logged) + ApiRequestError classification network/http/parse + probeService() healthcheck reachability probe
- [x] Pages (overview, payouts, sub-accounts): error banners now show the truthful failure reason via describeApiError (no swallowing, no fake data)
- [x] Settings: Test Connection per service (existing healthcheck endpoints; independent ✓/✗) + HTTPS→HTTP mixed-content detection & warning
- [x] Environment mapping regression check (tsc-compiled node run): local/testing/production client+transaction URLs all correct; default testing; key rvpay-dashboard-environment
- [x] npm run build PASS; npm run lint: same 4 pre-existing errors only; go build/vet/test ./clients/... ./transactions/... ./shared/... all pass (backend untouched this task)
- [x] Live curl/browser proof requires a running stack (DB + env) — exact commands documented in .dashboard-setup.md; CORS middleware chain order re-verified in both mains (CORS outermost)

## 2026-09-06 — Dashboard routes relocated under ALB-permitted prefixes
- [x] GET /v1/public/overview/snapshot → /v1/public/transactions/overview/snapshot (Transactions)
- [x] GET /v1/public/sub-accounts → /v1/public/clients/sub-accounts (Clients)
- [x] All other Dashboard routes verified already ALB-compatible (payouts* / payments* / deposits* / healthchecks, on both sides)
- [x] protobuf HTTP annotations updated; gateway code regenerated via protobuf/Makefile; old routes no longer registered (asserted in gateway tests)
- [x] lib/api.ts updated to new paths; env switching unchanged (path identical across environments)
- [x] Stale logging endpoint labels updated to real routes; cors_test paths updated
- [x] npm run build PASS (exit 0); npm run lint: 4 pre-existing errors only; go build/vet/test all pass

## 2026-09-08 — Administrator sign-in / access-token auth (APPENDED)
- [x] lib/auth.tsx NEW: AuthProvider + useAuth + AuthGuard; access/refresh tokens in sessionStorage (rvpay-dashboard-auth), never localStorage, never logged
- [x] app/sign-in/page.tsx NEW: email/password form → POST /v1/public/auth/sign-in; redirects to / on success
- [x] lib/api.ts: Authorization Bearer header attached per request; 401 triggers single-flight refresh (single-use rotation) + one retry, then session drop; signInRequest/signOutRequest added
- [x] AuthGuard wraps the protected pages: / (overview), /payouts, /sub-accounts; payment page, settings (healthchecks) and sign-in stay public
- [x] AuthProvider mounted in app/layout.tsx; sidebar shows the real signed-in admin (name/email) and the LogOut button calls the server sign-out then routes to /sign-in
- [x] Server-side enforcement is the backend middleware; the UI guard only prevents rendering protected UI (UI is not authorization)
- [x] npm run build verification (see command log)

## 2026-09-08 — Correction: database-managed administrators (APPENDED)
- [x] No dashboard changes required: sign-in/refresh/sign-out, AuthProvider/AuthGuard, sidebar identity and token storage unchanged and already user-agnostic (identity comes from the sign-in response, never hard-coded)
- [x] Multiple admins each see their own name/email; USER_ROLE_USER accounts cannot sign in to the dashboard (backend rejects)
