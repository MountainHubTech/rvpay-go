# Transactions PawaPay Checkpoint

## Status
Agent 00 (context preparation) COMPLETE. Findings recorded; handoff ready for Agent 01.

## Completed (Agent 00)
- Read 00-pawapay-context.md, transactions/.clinerules.md, agents 01-05, and all tracking files.
- Inspected legacy `deposits/` PawaPay wiring (config/model.go, cmd/grpc-service/main.go, deposits/service.go, service_test.go).
- Inspected Transactions deposits + payouts services and existing tests.
- Verified the target SDK API (v0.0.0-20260813104302-0841c026f9f4 = main HEAD) in the module cache; download confirmed.

## Current task
Each agent runs in a NEW task. Start by reading `.service-context.md` (full findings), `.service-checkpoint.md`
(this file), `.clinecheck.md`, and your own agents/*.md directive.

## Pending (agent order + instructions)
1. **Agent 01 (01-transactions-pawapay-bootstrap.md)**
   - Bump `go.mod` to `github.com/I-Frostbyte/pawapay_client v0.0.0-20260813104302-0841c026f9f4`; run `go mod tidy` (updates go.sum).
   - Add `PAWAPAY_API_URL` + `PAWAPAY_API_KEY` to `transactions/config/model.go` (`conf:"env:..."`) and to `transactions/.env.example`.
   - In `transactions/cmd/grpc-service/main.go`: `pawapayClient := pawapay_client.NewClient(config.APIURL, config.APIKey)`; pass `*pawapayClient` as the LAST arg to `NewDepositService` and `NewPayoutService`.
   - Keep server lifecycle intact. Update all three tracking files.
2. **Agent 02 (02-transactions-pawapay-deposits.md)**
   - `transactions/deposits/service.go`: add `pawapayClient pawapay_client.Client` field + constructor param (last).
   - After `s.depositRepo.Create(...)`, build `deposits.InitiateDepositRequest{DepositID: deposit.ID.String(), Amount: <decimal string>, Currency: currency, Payer: deposits.Payer{Type: "MMO", AccountDetails: deposits.AccountDetails{PhoneNumber: phoneNumber, Provider: <sqlc->pawapay>}}}` and call `s.pawapayClient.Deposits.InitiateDeposit(ctx, req)`.
   - Map provider errors to `codes.Internal` (legacy convention). Add `sqlc.PaymentProvider -> "MTN_MOMO_CMR"/"ORANGE_MOMO_CMR"` helper.
   - Update `service_test.go` constructor calls; make provider tests use `httptest`. Update tracking files.
3. **Agent 03 (03-transactions-pawapay-payouts.md)**
   - `transactions/payouts/service.go`: mirror Agent 02.
   - After `s.payoutRepo.Create(...)`, call `s.pawapayClient.Payouts.InitiatePayout(ctx, &payouts.InitiatePayoutRequest{PayoutID: payout.ID.String(), Amount: <decimal string>, Currency, Recipient: payouts.Recipient{Type: "MMO", AccountDetails: payouts.AccountDetails{PhoneNumber: <destination_reference>, Provider: <sqlc->pawapay>}}})`.
   - Provider error -> `codes.Internal`. Update tests. Update tracking files.
4. **Agent 04 (04-pawapay-tests-and-verification.md)**
   - `go test ./transactions/...`, `go vet ./transactions/...`, `gofmt` changed files.
   - Add only missing focused tests. NEVER call the real PawaPay API (use `httptest` + `NewClient(srv.URL, "key")`).
   - Update the existing `TestInitiateDepositSuccess` which reaches past `repo.Create` (would hit the real API).
   - Update tracking files.
5. **Agent 05 (05-final-review-and-readme.md)**
   - Append a PawaPay integration section to `transactions/README.md` documenting ONLY `PAWAPAY_API_URL` + `PAWAPAY_API_KEY` and the two integrated methods (and the destination_reference-as-phone assumption). Do not rewrite unrelated README content.
   - Record final verification here. Update tracking files.

## Decisions
- Bump `pawapay_client` to main HEAD `v0.0.0-20260813104302-0841c026f9f4`. This BREAKS legacy `deposits/` compilation - explicitly approved by the user. Do NOT modify `deposits/`.
- Scope limited to Transactions. Legacy `deposits/` is only the wiring/style guide.
- Only `PAWAPAY_API_URL` and `PAWAPAY_API_KEY` are allowed.
- Use only current SDK methods: `Deposits.InitiateDeposit` and `Payouts.InitiatePayout`.
- Do NOT implement DepositStatus, PayoutStatus, Resend*Callback, Toolkit, callbacks, reconciliation, concurrency/retries.
- Payout recipient phone number is assumed to be the `destination_reference` field.

## Risks / notes
- After the bump, `go test ./...` (repo-wide) will fail to build the legacy `deposits/` package. This is expected/accepted. Always validate with `go test ./transactions/...`.
- Provider is not mockable (no interface); use `httptest.Server`.
- Amount must be passed to the SDK as a decimal string, not `pgtype.Numeric`.
