# RVPay Go — Stage Checkpoint

## Stage: Minimal Secure Admin Authentication — COMPLETE (2026-09-08)
- [x] Clients: migration 000005 users+access_tokens (up/down), sqlc regen, user/access_token repos, clients/auth (argon2id, opaque hashed tokens, refresh rotation, sign-out, env-seeded bootstrap admin)
- [x] proto AuthService: /v1/public/auth/sign-in|refresh|sign-out + internal ValidateAccessToken; gateway regenerated via protobuf/Makefile
- [x] Transactions: auth middleware delegates validation to Clients via CLIENTS_GRPC_ADDR; protects overview/snapshot, payouts/stats, payouts list; deposits/payments/callbacks stay PUBLIC (payment-page exemption)
- [x] ALB rule respected: /v1/admin not routable → endpoints RETAIN /v1/public* prefixes with transport-layer middleware protection (§21 alternative); no ALB/infra edits
- [x] Dashboard: lib/auth.tsx (AuthProvider/AuthGuard), /sign-in page, api.ts Bearer + 401 single-flight refresh, sidebar real identity + sign-out; sessionStorage tokens, never logged
- [x] go build/vet/test ./clients/... ./transactions/... ./shared/... PASS; npm run build PASS (type-check clean); docs/checkpoints appended in all services
- [x] Correlation fix: shared/observability/cors.go Allow-Headers now "Content-Type, Authorization"; root cause of dashboard TypeError after auth = CORS allowed only Content-Type preflight while authenticated requests carry Authorization; single shared middleware fixes BOTH Clients:8080 and Transactions:8081; middleware order CORS→AdminAuth correct; preflight 204 transport-layer (no token needed); no env override for headers (origins only)
- [x] Regression: cors_test.go + transactions gateway_test preflight now send "authorization, content-type" and assert Allow-Headers "Content-Type, Authorization"; go build/vet PASS; go test ./clients/... ./transactions/... ./shared/... 22 pkgs ok, 0 failures

## Prior: Server-Connection Implementation — COMPLETE (2026-09-04) | HighLevel OAuth v3 — COMPLETE
## Next: deploy env (ADMIN_* + CLIENTS_GRPC_ADDR), migration 000005 on live DBs, restart services, live sign-in + protected-route verification