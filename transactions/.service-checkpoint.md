# Transactions PawaPay Checkpoint

## Status
COMPLETE. Agents 01-05 finished in a single task. The Transactions service now initiates PawaPay V2 deposits and payouts through the current `pawapay_client` SDK.

## Per-agent completed work

### Agent 01 — Bootstrap (01-transactions-pawapay-bootstrap.md)
- Bumped `github.com/I-Frostbyte/pawapay_client` in `go.mod` to `v0.0.0-20260813104302-0841c026f9f4` and added the matching `go.sum` entries via `go mod download github.com/I-Frostbyte/pawapay_client@v0.0.0-20260813104302-0841c026f9f4`.
- Added `APIURL` (`conf:"env:PAWAPAY_API_URL"`) and `APIKey` (`conf:"env:PAWAPAY_API_KEY"`) to `transactions/config/model.go`.
- Added `PAWAPAY_API_URL` and `PAWAPAY_API_KEY` to `transactions/.env.example`.
- In `transactions/cmd/grpc-service/main.go`: imported `pawapay_client`, built `pawapayClient := pawapay_client.NewClient(config.APIURL, config.APIKey)`, and passed `*pawapayClient` as the LAST arg to `deposits.NewDepositService` and `payouts.NewPayoutService`. Server lifecycle unchanged.

### Agent 02 — PawaPay Deposits (02-transactions-pawapay-deposits.md)
- `transactions/deposits/service.go`: added `pawapayClient pawapay_client.Client` field and last constructor param.
- After `s.depositRepo.Create(...)`, added `initiatePawapayDeposit` which builds `pawapaydeposits.InitiateDepositRequest{DepositID, Amount (decimal string), Currency, Payer{Type:"MMO", AccountDetails{PhoneNumber, Provider}}}` and calls `s.pawapayClient.Deposits.InitiateDeposit(ctx, req)`.
- Added `sqlcPaymentProviderToPawapay` helper (`MTN_MOMO` -> `MTN_MOMO_CMR`, `ORANGE_MOMO` -> `ORANGE_MOMO_CMR`).
- Provider errors map to `codes.Internal` (legacy convention).

### Agent 03 — PawaPay Payouts (03-transactions-pawapay-payouts.md)
- `transactions/payouts/service.go`: added `pawapayClient pawapay_client.Client` field and last constructor param.
- After `s.payoutRepo.Create(...)`, added `initiatePawapayPayout` which builds `pawapaypayouts.InitiatePayoutRequest{PayoutID, Amount (decimal string), Currency, Recipient{Type:"MMO", AccountDetails{PhoneNumber: destination_reference, Provider}}}` and calls `s.pawapayClient.Payouts.InitiatePayout(ctx, req)`.
- Added the same `sqlcPaymentProviderToPawapay` helper. Provider errors map to `codes.Internal`.

### Agent 04 — Tests and Verification (04-pawapay-tests-and-verification.md)
- `transactions/deposits/service_test.go` and `transactions/payouts/service_test.go`: updated all constructor calls with the new client arg; validation/zero-value/customer-not-found/duplicate tests use `pawapay_client.Client{}`.
- Success tests (`TestInitiateDepositSuccess`, `TestRequestPayoutSuccess`) now use `httptest.NewServer` + `pawapay_client.NewClient(srv.URL, "test-key")` so they never call the real API.
- Added provider-error tests (`TestInitiateDepositProviderError`, `TestRequestPayoutProviderError`) returning HTTP 500 and expecting `codes.Internal`.
- Commands: `go build ./transactions/...`, `go test ./transactions/...`, `go vet ./transactions/...`, and `gofmt -w` on changed files — all pass; `gofmt -l` clean.

### Agent 05 — Final Review and README (05-final-review-and-readme.md)
- Appended a "PawaPay Integration" section to `transactions/README.md` documenting only `PAWAPAY_API_URL` / `PAWAPAY_API_KEY`, the two integrated methods, and the `destination_reference`-as-phone assumption. Unrelated README content untouched.
- Marked all checklist items done in `.clinecheck.md`.

## Verification
- `go build ./transactions/...` passes.
- `go test ./transactions/...` passes (all packages).
- `go vet ./transactions/...` passes.
- `gofmt -l` clean on all changed Go files.

## Decisions
- Bump `pawapay_client` to main HEAD `v0.0.0-20260813104302-0841c026f9f4`. This BREAKS legacy `deposits/` compilation (repo-wide `go mod tidy` fails because legacy `deposits/` imports the removed `pawapay_client/config`) - explicitly approved by the user. `deposits/` was NOT modified.
- Scope limited to Transactions. Legacy `deposits/` used only as the wiring/style guide.
- Only `PAWAPAY_API_URL` and `PAWAPAY_API_KEY` are allowed.
- Only current SDK methods used: `Deposits.InitiateDeposit` and `Payouts.InitiatePayout`.
- No DepositStatus, PayoutStatus, Resend*Callback, Toolkit, callbacks, reconciliation, or retries.
- Payout recipient phone number is assumed to be the `destination_reference` field.
- SDK deposits/payouts packages are aliased (`pawapaydeposits` / `pawapaypayouts`) to avoid colliding with the local `deposits`/`payouts` packages.
- The validated local `provider`/`phoneNumber` variables are used for the SDK request (not the possibly-empty fields on the repo-returned struct).

## Risks / notes
- Repository-wide `go test ./...` will fail to build the legacy `deposits/` package after the bump. Expected/accepted. Validate with `go test ./transactions/...`.
- Provider is not mockable (no interface seam); tests use `httptest.Server` + `NewClient(srv.URL, "test-key")`.
- Amount is passed to the SDK as a decimal string, not `pgtype.Numeric` or `float64`.

---

## Follow-up: Legacy deposits service aligned with bumped pawapay_client

### Context
The legacy `deposits/` service still referenced the old `pawapay_client/config` API
(`model.Deposit`, `DepositsService.InititateDeposit`), which the bumped SDK
(`v0.0.0-20260813104302-0841c026f9f4`) removed. This broke repo-wide compilation and `go mod tidy`.

### Changes
- `deposits/deposits/service.go`
  - Removed `model "github.com/I-Frostbyte/pawapay_client/config"` import.
  - Added `pawapaydeposits "github.com/I-Frostbyte/pawapay_client/deposits"` and `strconv`.
  - Replaced the `model.Deposit` + `DepositsService.InititateDeposit(&deposit)` call with
    `pawapay.Deposits.InitiateDeposit(ctx, &pawapaydeposits.InitiateDepositRequest{...})`,
    passing the amount as a decimal string via `strconv.FormatFloat(amount.Float64, 'f', 2, 64)`.
- `deposits/deposits/service_test.go`
  - Added `TestInitiateDepositSuccess` (using `httptest.Server` + `NewClient(srv.URL, "test-key")`
    with mocked `DepositsRepo`/`Querier`) verifying the new deposit request path.
  - Added `TestInitiateDepositProviderError` (HTTP 500) expecting `codes.Internal`.
- `deposits/cmd/grpc-service/main.go`
  - No change required; it already constructs `pawapay_client.NewClient(config.APIURL, config.APIKey)`
    and passes `*pawapayClient` to `deposits.NewDepositsService`.

### Verification
- `go build ./...` passes.
- `go test ./...` passes (all services, including `deposits/deposits`).
- `go vet ./...` passes.
- `gofmt -l` clean on changed Go files.
- `go mod tidy` now succeeds; `go.sum` retains only the new `pawapay_client` version.

### Outcome
Repository-wide build and tests are green again; the follow-up identified in the
transactions integration checkpoint (legacy `deposits/` breakage) is resolved.

## 2026-09-02 — HighLevel external deposit identifiers
- Status: COMPLETE
- Proto: client_id→client_name in CreateDepositRequest/Deposit; grpc/go regenerated
- Migration: 000003_external_deposit_identifiers (UUID→TEXT, customer_id/merchant_id nullable, FKs dropped, down migration included)
- Service/repo/sqlc/tests updated; uuid.Parse removed from deposit path; PawaPay + GHL verify flows untouched
- go build/vet/test PASS; limitations: live DB migration + live GHL flow unverified


## 2026-09-02 — Nullable text scan fix
- Status: COMPLETE
- Root cause: sqlc.yaml nullable-text override → string (scan NULL failure on external_reference)
- Fix: removed override; sqlc regen → *string; repo textPtr/textRef adapters; converters deref at wire layer only; Case1-3 tests added
- No migration, no proto change, no page.tsx change


## 2026-09-02 — VerifyPayment GET + transactional InitiateDeposit
- Status: COMPLETE
- VerifyPayment: POST→GET, query-param binding (fields unchanged), gateway regenerated
- InitiateDeposit: single DB tx around INSERT + PawaPay; failure → rollback; commit failure → Internal
- Decision: reused existing TransactionsRepo.Begin(ctx) (Querier, Tx) mechanism; no new frameworks


## 2026-09-02 — HighLevel transactionId mapping correction
- Status: COMPLETE
- Corrected mapping: client_name←highlevel-<locationId>; customer_id←contact.id; merchant_id←payer phone number (temporary); ghl_transaction_id←HighLevel transactionId (new proto field 8, existing DB column)
- VerifyPayment resolves via deposits.ghl_transaction_id (charge fallback preserved); GET contract unchanged


## 2026-09-02 — PawaPay initiation status handling
- Status: COMPLETE
- Only status=ACCEPTED allows the deposit tx to commit; REJECTED (HTTP 200 + failureReason) and unexpected statuses roll back and return Internal; transport errors unchanged
- Rejection log includes deposit_id, failure_code, failure_message, provider, phone, amount, currency (no secrets)
- Phone number confirmed MSISDN (no +) in serialized request; debug Println removed; tests added for REJECTED/UNKNOWN_STATUS/500


## 2026-09-02 — Zero-decimal XAF amounts
- Status: COMPLETE
- XAF amounts serialized whole-number (25.00→"25"); fractional XAF rejected exactly (big.Int mantissa) before PawaPay; other currencies unchanged


## 2026-09-02 — PawaPay deposit callback
- Status: COMPLETE (endpoint + business logic + tests)
- Route: POST /v1/public/pawapay/deposits/callback → PaymentService.ProcessDepositCallback
- Correlation: deposits.id (the UUID sent to PawaPay as depositId); VerifyPayment correlation via ghl_transaction_id unchanged
- Security: no app auth on this route (PawaPay cannot present RVPay credentials); signed-callback (RFC-9421) verification deferred until confirmed enabled in the PawaPay Dashboard — no custom signature scheme invented


## Admin Dashboard Server-Connection Mapping — Transactions (2026-09-04, read-only)
- Status: COMPLETE (mapping only; no code/SQL/proto changes in transactions/)
- Reused endpoints (verified against protobuf/transactions.proto + protobuf/common.proto):
  - POST /v1/public/deposits (InitiateDeposit) — dashboard body matches CreateDepositRequest exactly: client_name="highlevel-<locationId>", customer_id=contact.id, merchant_id=payer phone (temporary), ghl_transaction_id=HighLevel transactionId, amount=Money{decimal string, FCFA→XAF}, payment_type=PAYMENT_TYPE_MMO, payer_phone_number, provider (PROVIDER_MTN_MOMO/PROVIDER_ORANGE_MOMO). No dashboard correction needed.
  - GET /v1/public/payments/verify (VerifyPayment) — dashboard sends ghlTransactionId/ghlChargeId/subscriptionId query params and consumes {success, failed}; matches exactly.
- Dashboard request corrections: none.
- Additive response fields: none required while /payment is the only live page.
- Genuinely missing endpoints (DELEGATED — implement only when the pages are activated):
  1. GET /api/v1/overview/snapshot?period=Last 7|30|90 Days|This Year → {statCards[], revenueOverTime[], needsAttention[], recentPayouts[]}. Requires new aggregate read queries over deposits/payouts (no current read endpoint provides aggregates or time-bucketed revenue).
  2. GET /api/v1/payouts/overview/stats → pending/cleared/failed totals. No current aggregate endpoint.
  3. GET /api/v1/payouts?search=&status=Failed|In Transit|Cleared|Pending&page=&pageSize= → {rows[{id,name,location,initials,avatarClass or raw data,amount,status,initiated,expectedOrCleared,expectedOrClearedStrong}],total,page,pageSize}. Current payouts API only supports CreatePayout/GetPayout (single fetch); no paginated list with account metadata/total count. Payout list/enrichment is additive over the existing Payout message; do not alter PawaPay initiation/status behavior.
  4. (future) GET /api/v1/payouts/export and GET /api/v1/payouts/{id} for CSV/row expansion.
- Not currently required: /transactions, /disputes, /settings pages are placeholders with zero requests; notification bell inert.
- Tests required when implemented: pagination/filtering/sorting unit tests per existing repo conventions; aggregates vs live DB correctness; no real PawaPay calls in tests.

## 2026-09-04 — Customer Transactional Flow + client_name Migration
- Status: COMPLETE
- Migration: 000004_customer_client_name.up.sql / .down.sql — customers.client_id(UUID)→client_name(TEXT, preserved as canonical text); merchant_id→nullable; +name TEXT, +address TEXT; unique(client_name, phone_number); indexes rebuilt. Down migration valid only while stored client_name values are UUID text (same caveat as 000003). NO FK customer.client_name→clients.client_name: clients.client_name is not unique, so client scoping is enforced at repository/service level (documented).
- Schema propagation: SQL → sqlc (v1.29.0, pinned) → repo → service → proto → gateway. Nullable uuid now emitted as pgtype.UUID (removed the nullable-uuid override) so SQL NULL scans correctly.
- Proto (transactions.proto, additive): Customer gains client_name(2, renamed from client_id), name(8), address(9); CreateCustomerRequest gains client_name(1, renamed), name(4), address(5), merchant_id now optional; CreateDepositRequest gains customer(9). grpc/go regenerated via protobuf/Makefile.
- Customer flow (InitiateDeposit): BEGIN → resolve-or-create Customer (GetByClientNameAndPhone, else Create with ON CONFLICT DO NOTHING; concurrent-duplicate handled) → deposit INSERT → PawaPay InitiateDeposit → COMMIT only on ACCEPTED; REJECTED/unexpected/transport/customer-failure/deposit-failure/commit-failure all ROLLBACK (Customer + Deposit atomic). Reuses existing TransactionsRepo.Begin mechanism; no new framework.
- PawaPay behavior preserved: ACCEPTED-only commit, XAF whole-number, provider mapping, phone MSISDN, callback correlation by deposits.id, VerifyPayment GET unchanged.
- Tests: transactions/deposits/service_customer_test.go (9 cases: ACCEPTED commit, REJECTED rollback, unexpected-status rollback, customer-failure no deposit, deposit-failure rollback, commit-failure Internal, client_name association, field assertions, existing-customer reuse) + updated service_test.go constructor + customers/service_test.go for client_name. go test ./transactions/... -count=1 PASS; gofmt clean on touched files.
- Remaining (FUTURE, not implemented): overview snapshot, payouts stats/list, sub-accounts list/create, /me (blocked on auth). Documented in admindashboard/.dashboard-setup.md.

## Admin Dashboard data endpoints (2026-09-04)
- Status: COMPLETE
- Endpoints (v1/public convention preserved):
  1. GET /v1/public/overview/snapshot?period=7d|30d|90d|ytd → DashboardOverviewService (new transactions/overview package): total_revenue (COMPLETED deposits sum), transaction_volume (deposit count), revenue_over_time (per-day buckets, sparse — no zero-fill), recent_payouts (latest 5). active_sub_accounts + needs_attention intentionally unpopulated (Clients-owned tenancy data / disputes not a current concept) — additive fields documented in proto.
  2. GET /v1/public/payouts/overview/stats → PayoutService: Total Pending (REQUESTED+PROCESSING counts+sums), Total Cleared, Failed Payouts.
  3. GET /v1/public/payouts?search=&status=&page=&pageSize= → PayoutService: paginated rows (initials from destination reference, money formatting, status mapping REQUESTED/PROCESSING→Pending, COMPLETED→Cleared, FAILED→Failed), total for pagination. Max page size 100.
- Money handling: NUMERIC(18,2) aggregates decoded via pgtype.Numeric (sqlc interface{} + numericFromInterface helper); display-only float formatting.
- sqlc v1.29.0 regenerated (deposits + payouts queries, querier, mocks). No migration needed (existing columns only). No existing endpoint or PawaPay behavior changed.
- Verification: go build/vet/test ./transactions/... -count=1 PASS; gofmt clean.
- Remaining: overview needsAttention requires a dispute concept; activeSubAccounts requires cross-service aggregation with Clients (documented, not fabricated).

## Migration 000004 local-run failure fix (2026-09-04)
- Failure: pq: relation "idx_customers_merchant_id" already exists — the index was created in 000001_init_schema.up.sql (btree on merchant_id) and is unaffected by 000004's client_id rename and merchant_id DROP NOT NULL, so 000004's plain CREATE INDEX was a redundant duplicate.
- Correction: transactions/db/migrations/000004_customer_client_name.up.sql — the merchant_id index statement is now CREATE INDEX IF NOT EXISTS (comment documents why). Intended schema unchanged: client_id→client_name TEXT, +name/+address, merchant_id nullable, idx_customers_client_name, unique (client_name, phone_number), existing merchant_id index preserved. Down migration unchanged (it drops the index before recreating it).
- DB recovery note: the failed run left schema_migrations at version=4/dirty=true (golang-migrate bookkeeping; the migration's DDL had rolled back inside its transaction). The version table was manually reset to 3/false before re-running; verified pre-000004 schema was intact and customer table had 0 rows (no data changed manually).
- Verification: make run reached successful startup (Migrations applied successfully; gRPC :50052; gateway :8080); post-migration schema inspected (columns/types/nullability, exactly one idx_customers_merchant_id, both new indexes, unique constraint, version=4/dirty=false); go test ./transactions/... -count=1 PASS; go vet clean; no Go files changed.
