# .clinecheck — Agent 02 task plan (HighLevel Marketplace OAuth Fix)

## 1. INSTALL establishes RVPay tenancy
- [ ] Resolve HighLevel platform via `PlatformRepo.GetBySlug("highlevel")`; NEVER create.
- [ ] Resolve/reuse client for the GHL sub-account; create when missing.
- [ ] Resolve/reuse integration (`client_id` + platform); create when missing with
      `external_account_id = locationId`.
- [ ] Make INSTALL idempotent (repeated INSTALL reuses existing records).
- [ ] Ensure `webhooks/service.go` INSTALL path creates client+integration BEFORE the
      `webhook_events` insert (FK `integration_id` dependency) — currently it errors
      `ErrIntegrationNotFound` when neither `external_account_id` nor config resolves.
- [ ] Wire `clientsRepo` + `platformRepo` into `HighLevelWebhookDispatcher`
      (constructor currently only has integrationsRepo + configRepo).
- [ ] Still create/find `payment_provider_configs` idempotently (unchanged intent).

## 2. OAuth callback resolves integration by locationId
- [ ] Confirm no-state `HandleCallback` resolves via
      `external_account_id` then `payment_provider_configs.location_id`.
- [ ] Pass the already-exchanged token response into downstream processing.

## 3. Exchange code exactly once
- [ ] Fix double exchange: no-state `HandleCallback` exchanges, then calls
      `ProcessCallback(..., code, ...)` which exchanges AGAIN. Stop re-exchanging;
      pass `*TokenResponse` downstream using existing types/smallest change.
- [ ] Keep state-based flow (single exchange in `ProcessCallback`) intact.

## 4. Tests
- [ ] INSTALL creates client + integration.
- [ ] Repeated INSTALL reuses existing records.
- [ ] Integration maps to HighLevel `locationId`.
- [ ] Marketplace callback resolves the integration by `locationId`.
- [ ] Authorization code exchanged exactly once.
- [ ] Existing state-based callback behavior remains intact.
- [ ] Repository/SQL behavior covered with existing test conventions.

## 5. Verification
- [ ] Run narrow changed-package tests, then the clients-service suite.
- [ ] Inspect full diff for unrelated changes.
- [ ] Update `.clinecheck.md` (checked/unchecked) and `.service-checkpoint.md`.