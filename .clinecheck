# RVPay Go — Stage Checkpoint

## Stage: Server-Connection Implementation — COMPLETE (2026-09-04)
- [x] Customer transactional flow: migration 000004 (client_id→client_name + name/address), proto customer=9, resolve-or-create inside deposit tx, ACCEPTED-only commit, 9 rollback tests
- [x] Dashboard LIVE endpoints: POST /v1/public/deposits(+customer), GET /v1/public/payments/verify, GET /v1/public/overview/snapshot, GET /v1/public/payouts/overview/stats, GET /v1/public/payouts, GET /v1/public/sub-accounts (all v1/public convention)
- [x] Dashboard wired: lib/api.ts (NEW), dashboard-home, payouts page/overview, sub-accounts page/table fetch live; gaps render "—" (no fabrication)
- [x] .dashboard-setup.md LIVE/FUTURE classified; all 6 checkpoint files appended; clients + transactions mocks/test-doubles updated
- [x] go build/vet/test ./clients/... ./transactions/... PASS; gofmt clean (CRLF baseline preserved); dashboard tsc/next build env-blocked (incomplete node_modules — pre-existing)
- [x] FUTURE documented: POST /sub-accounts (UI inert), payouts export/detail, transactions/disputes/settings pages, GET /me (auth-blocked)

## Prior: HighLevel OAuth v3 Migration — COMPLETE | Server-Connection Mapping audit — COMPLETE
## Next: npm run build in a normal Node env; live GHL install + payment end-to-end test; cross-service gaps (sub-account metrics, needsAttention) when enabled