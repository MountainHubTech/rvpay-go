# Cline Ignore Scope

Ignore unless explicitly required:
- `grpc/go/`
- `transactions/db/sqlc/`
- `deposits/db/sqlc/`
- `third_party/`
- `.git/`
- build output
- Docker caches

Unrelated services:
- `clients/`
- `integrations/`
- `admindashboard/`
- all of `deposits/` except the specific legacy PawaPay files needed as a guide

Unrelated infrastructure:
- deployment templates
- CI deployment files
- cloud infrastructure

Do not modify ignored files.
