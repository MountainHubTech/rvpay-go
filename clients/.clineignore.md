# .clineignore — clients/ context boundary (Agent 02)

Keep reads inside `clients/` only, and only the files that affect the
HighLevel Marketplace INSTALL / OAuth callback change.

## Read (allowed)
- `cmd/grpc-service/main.go` — service wiring (entry point; how repos/services are injected)
- `config/model.go` — env bindings used by OAuth/webhook
- `oauth/service.go`, `oauth/errors.go` — callback/token-exchange logic
- `webhooks/service.go` — ProcessWebhook, INSTALL resolution, event idempotency
- `providers/provider.go`, `providers/webhook.go`, `providers/registry.go`,
  `providers/highlevel.go`, `providers/highlevel_webhook.go`
- `service/integrations_service.go` (existing InstallIntegration conventions),
  `service/clients_service.go`, `service/platforms_service.go`
- `http/oauth_handler.go`, `http/webhook_handler.go`
- `db/migrations/000001_init_schema.{up,down}.sql`, `000002_oauth_states.*`,
  `000004_payment_provider_configs.*`
- `db/query/clients.sql`, `integrations.sql`, `platforms.sql`,
  `oauth_states.sql`, `oauth_tokens.sql`, `payment_provider_configs.sql`
- `db/repo/client_repo.go`, `integration_repo.go`, `platform_repo.go`,
  `oauth_state_repo.go`, `oauth_token_repo.go`, `payment_provider_config_repo.go`,
  `errors.go`
- `db/sqlc/*.go` — generated signatures only (models, querier) to confirm field names
- Existing `*_test.go` for the above ONLY to match test conventions / debug failures

## Ignore / do not open
- Everything outside `clients/` (transactions/, deposits/, payments (root pkg under clients is OK), pawaPay, etc.)
- `clients/payments/`, `clients/health/` — not in the INSTALL/OAuth call path
- `cmd/grpc-service/gateway_test.go`, `runtime_test.go` unless a directly affected failure
- Generated / dependency trees: `grpc/go/`, `third_party/`, `vendor/`, `node_modules/`, `.git/`, build output
- `db/sqlc/` internals (only signature references)
- `.git/`, any `.env`, secrets
- Non-HighLevel provider files not in the call path

Note: `payments/{service,errors}.go` and `providers/highlevel_payment_provider.go`
are for the GHL Custom Payment endpoints, NOT the  Marketplace
INSTALL/OAuth callback. Read only if the checkpoint marks them affected.

---

# .clineignore — Agent 03/04: HighLevel Marketplace v3 boundary

Marked append for the GHL v3 audit (Agent 03, read-only) and implementation
(Agent 04). Supersedes the Agent 02 boundary above for the v3 work; Agent 02
rules remain in force unless changed here.

## Read (allowed) — GHL v3 call path + direct deps
- `providers/highlevel_payment_provider.go` — Custom Payment Provider client
  (Agent 04 PRIMARY fix surface: association + connect payloads / locationId)
- `providers/payment_provider.go`, `providers/provider.go`,
  `providers/registry.go`, `providers/webhook.go`
- `providers/highlevel.go` — OAuth token exchange/refresh, scopes
- `providers/highlevel_webhook.go` — webhook dispatcher + integration-installed
- `oauth/service.go`, `oauth/errors.go` — callback + RegisterProvider flow
- `webhooks/service.go`, `webhooks/errors.go` — INSTALL provisioning
- `payments/service.go`, `payments/errors.go` — query + payment webhook
  (uses provider_api_key / location_id); NOT the v3 connection contracts
- `http/{oauth_handler,webhook_handler,payment_query_handler,payment_webhook_handler}.go`
- `service/{clients,integrations,platforms}_service.go`, `service/converters.go`,
  `service/errors.go`
- `config/model.go`, `config/model_test.go`
- `db/query/*.sql` and `db/migrations/*.up.sql` — READ-ONLY; never edit in the audit
- `db/repo/*.go` + `db/sqlc/{models,querier,*.sql}.go` — READ-ONLY signatures;
  never hand-edit generated `db/sqlc/*` or `grpc/go/*`
- Existing `*_test.go` for the above ONLY for conventions / updating tests
- `clients/README.md`, `clients/.service-checkpoint.md`

## Ignore / do not open
- `${workspace}/transactions/`, `deposits/`, legacy `integrations/`, `$ROOT`
  platform/`shared/`, `grpc/go/`, `third_party/`, `protobuf/`, `infra/`,
  `deploy/`, `vendor/`, `node_modules/`, `.env`, `.git/`, build output
- `clients/health/` — not on the v3 install/registration path
- Non-HighLevel providers
- Root `README.md` / root `.clinerules.md` / `.clineignore` — conventions only

## Audit constraint (Agent 03)
- This Agent 03 task is READ-ONLY. The only files that may be mutated are the
  four Agent 03 deliverables: `.clineignore.md`, `.clinerules.md`,
  `.clinecheck.md`, `.service-checkpoint.md`. No Go / SQL / migration changes.
- Never read credentials/codes (client_secret, access_token, refresh_token,
  OAuth `code`, apiKey) into this context; never log them.