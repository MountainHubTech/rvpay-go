# Cline Check — Dockerization Sequence Verification

Lightweight verification checklist for the entire Dockerization sequence. Agents may mark only checks they actually verified.

## Bootstrap

- [x] Bootstrap complete (Agent 00)
- [x] Containerization reconnaissance complete (Agent 01)
- [x] Application configuration valid (Agent 02)
- [x] Docker build valid (Agent 03)
- [x] Production container starts (Agent 03/06)
- [x] Runtime environment handling verified (Agent 04)
- [x] Makefile workflow valid (Agent 05)
- [x] Production image is appropriately minimal (Agent 03/06)
- [x] No secrets copied into the image (Agent 03/04/06)
- [x] Final AWS-readiness assessment complete (Agent 06)

## Payment Checkout (paymentsUrl iframe) — HighLevel flow fix

- [x] Payment page guard fixed: only `transactionId` required (apiKey is server-side)
- [x] Query body sends documented `type: "verify"`
- [x] Amount prefilled from `amount` query param when present
- [x] Syntax check passes (esbuild transform; full tsc/build blocked by sandbox node_modules)

## Payment Checkout — payment_initiate_props postMessage contract

- [x] `ready` event posted to parent on iframe mount (GHL handshake)
- [x] `payment_initiate_props` message listener captures amount/currency/transactionId/orderId/contact
- [x] transactionId now sourced from the event (URL params kept as fallback)
- [x] Amount, currency→country, and customer phone prefilled from the event
- [x] Syntax check passes (esbuild transform)

## Agent 00-payment-page-agent — transactionId authority + real initiation

- [x] payment_initiate_props SUSPENDED (kept as diagnostics/fallback, non-blocking)
- [x] transactionId read authoritatively from the payment URL query string
- [x] Pay button calls the EXISTING `POST /v1/public/deposits` initiation endpoint
- [x] Status polling moved to the EXISTING `GET /v1/public/payments/verify`
- [x] No fabricated IDs; duplicate submissions blocked by isSubmitting
- [x] `[RVPay]` iframe diagnostics and `custom_provider_ready` retained
- [x] Syntax check passes (esbuild; full build still sandbox-blocked)

## Surgical correction — buyNowProductId removal + ungated initiation

- [x] `buyNowProductId` no longer mapped as transactionId (explicit code note)
- [x] No transactionId fabricated; unavailable = null
- [x] `if (!transactionId)` gate removed from handlePay
- [x] Pay reaches existing `POST /v1/public/deposits` regardless of GHL id
- [x] GHL verify polling only when a REAL transactionId exists
- [x] Amount validation added; backend error surfaced honestly
- [x] HTTP 200 ≠ fake success; no fabricated polling path
- [x] `[RVPay]` diagnostics + `custom_provider_ready` preserved
- [x] Syntax check passes (esbuild transform)




## Payment page redo — working /payment/checkout handshake reproduced

- [x] Working implementation protocol inspected and identified (retried, stringified ready; JSON-string message parsing; both props events)
- [x] `message` listener registered before the ready post
- [x] Ready message JSON-stringified with `loaded: true` + `addCardOnFileSupported: false`
- [x] Ready message retried on ~500ms interval; stops on payment-context arrival; cleared on unmount
- [x] Listener safely parses JSON-string AND object `event.data`; malformed messages ignored
- [x] `payment_initiate_props` and `setup_initiate_props` both accepted; real transactionId captured (orderId never substituted)
- [x] transactionId preferred from the event; genuine URL param kept as fallback; never fabricated
- [x] Fail-safe: "Invalid payment request. Missing payment transaction information." when no real ID after both sources
- [x] Existing `POST /v1/public/deposits` initiation and `GET /v1/public/payments/verify` polling unchanged; no invented endpoint/field
- [x] UI, `[RVPay]` diagnostics, duplicate-submission guard, single polling interval preserved
- [x] Syntax check passes (esbuild TSX transform; full tsc/build still environment-blocked)

## 2026-09-02 — HighLevel payment identifier mapping fix
- [x] initiatePayment now sends: clientName=`highlevel-${locationId}` (existing RVPay client-name convention), customerId=`payment_initiate_props.contact.id`, merchantId=`payment_initiate_props.transactionId`
- [x] Fails safely when locationId / contact.id / transactionId are missing; no identifiers fabricated; old clientId/customerId/merchantId URL-param mapping removed
- [x] HighLevel `payment_initiate_props` iframe handshake untouched; payment fields (amount, currency, paymentType, payerPhoneNumber, provider) unchanged
- [x] Matches regenerated CreateDepositRequest contract (client_name=1, customer_id=2, merchant_id=3)
- [ ] FULL tsc/next build could NOT run here (WSL; node_modules incomplete, only Windows Node available) — verified by source inspection only; run `npm run build` in the Windows environment

## 2026-09-02 — VerifyPayment GET contract (backend)
- Backend now serves GET /v1/public/payments/verify?ghlTransactionId=... (POST mapping removed); dashboard polling unchanged, no 501 expected after deploy


## 2026-09-02 — HighLevel transactionId mapping correction
- page.tsx now sends merchantId=<payer phone number> and ghlTransactionId=<genuine HighLevel transactionId>; transactionId no longer mapped to merchantId; handshake and fail-safe behavior unchanged


## 2026-09-02 — PawaPay callback backend
- Backend endpoint added: POST /v1/public/pawapay/deposits/callback (no auth, idempotent, 200 on success/duplicates); dashboard code untouched


## 2026-09-02 — Payment polling semantics fix
- [x] Polling keeps GET /v1/public/payments/verify (queryPaymentStatus unchanged), interval now the PAYMENT_POLL_INTERVAL_MS constant (3000ms)
- [x] Pending semantics fixed: only failed === true is terminal failure; success === false alone no longer stops polling; both false → keep polling
- [x] Temporary fetch/network/server errors: logged via console.warn and polling continues inside the window
- [x] 10-minute timeout via PAYMENT_POLL_TIMEOUT_MS + pollDeadlineRef; timeout stops polling, sets isSubmitting(false), shows the timeout message, never re-initiates
- [x] stopPolling() clears interval + deadline; unmount effect clears both; single active interval guaranteed (stopPolling before starting)
- [x] Duplicate submissions still blocked by isSubmitting while polling; initiation/handshake/identifiers untouched
- [x] Syntax validated (esbuild TSX transform, no errors); full tsc/next build still environment-blocked (broken Linux node_modules, Windows-only Node) — run npm run build on Windows
