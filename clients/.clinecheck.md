# .clinecheck — Agent 03: HighLevel Marketplace v3 AUDIT (read-only)

Audit outcome. No code, SQL, or migration changes were made. Only the four
Agent 03 deliverables (`.clineignore.md`, `.clinerules.md`, `.clinecheck.md`,
`.service-checkpoint.md`) were touched. Full load for Agent 04.

---

## 1. Files inspected
- `clients/providers/highlevel_payment_provider.go`, `payment_provider.go`,
  `provider.go`, `registry.go`, `webhook.go`, `highlevel.go`,
  `highlevel_webhook.go`
- `clients/oauth/service.go`, `oauth/errors.go`
- `clients/webhooks/service.go`, `webhooks/errors.go`
- `clients/payments/service.go`, `payments/errors.go`
- `clients/http/{oauth_handler,webhook_handler,payment_query_handler,payment_webhook_handler}.go`
- `clients/service/{clients,integrations,platforms}_service.go`, `service/converters.go`
- `clients/db/query/*.sql`, `db/migrations/{000001_init_schema,000004_payment_provider_configs}.up.sql`
- `clients/db/repo/{client,integration,payment_provider_config,errors}.go`,
  `clients/db/sqlc/models.go`
- `clients/config/model.go`, `clients/cmd/grpc-service/main.go`
- `clients/README.md`, `clients/.clinecheck.md`, `clients/.service-checkpoint.md`
- `agents/project-context.md`
- Official v3 docs (marketplace.gohighlevel.com): oauth-2-0-v-3, get-access-token,
  payments create-integration, create-config, fetch-config, marketplace-modules/Payments

## 2. Dependency / read-depth map and INSTALL/OAuth/registration path
```
GHL `/oauth/callback` (code[, state])
  → http/oauth_handler.go → oauth.Service.HandleCallback(code, state)
      ├─ state="" (Marketplace): ExchangeCode→locationId → GetBySlug("highlevel")
      │     → tenant client (idempotent) → integration (idempotent)
      │     → processCallbackWithToken (SINGLE exchange preserved, no re-exchange)
      └─ state!="" : OAuthStatePool.Consume → ProcessCallback → ExchangeCode (once)
            → processCallbackWithToken
  → processCallbackWithToken: activate integration → oauthRepo.Create(token)
      → capability payment_provider → oauth.RegisterProvider (best-effort)
          → provider.PaymentProvider()
             ├─ CreateProviderAssociation POST /payments/custom-provider/provider
             ├─ CreateProviderConfig      POST /payments/custom-provider/connect
             └─ FetchProviderConfig       GET  /payments/custom-provider/connect
          → configRepo.Create/Update (local payment_provider_configs)

INSTALL: POST /webhooks/highlevel → webhooks.Service.ProcessWebhook
  → HighLevelWebhookDispatcher.handleIntegrationInstalled → resolve tenant by
    locationId → configRepo.Create (empty metadata; apiKey generated later)
OAuth token URL/scope: providers/highlevel.go ExchangeCode/RefreshToken
```
Read-depth: `providers/highlevel_payment_provider.go` → READ-DEEP (fix surface).
`oauth/service.go`, `providers/highlevel.go` → READ-DEEP. `webhooks/*`,
`payments/*`, `db/{query,repo,migrations}`, `config`, `main.go` → READ-SHALLOW.
`grpc/go/`, legacy `integrations/`, `transactions/`, `shared/`, `infra/` → AVOID.
`db/sqlc/*.go`, `grpc/go/*` → GENERATED (never hand-edit).

## 3. DB findings (read-only postgres against local `clients-postgres`)
- `platforms`: one row `slug='highlevel'` (name `GOHIGHLEVEL`, enabled=t)
  → resolve by slug; NEVER create a platform row.
- `clients`: 1 row `highlevel-teXi7QdXXCpgQOzqaVUN` (ACTIVE) — the dev tenant.
- `integrations`: 1 row `external_account_id = teXi7QdXXCpgQOzqaVUN`, ACTIVE.
  - Unique: `uq_integration_client_platform(client_id, platform_id)`.
  - `external_account_id` is a NON-unique index — single-location assumption
    holds only because the client name derives from the locationId.
- `payment_provider_configs`: 1 row with `provider_name=EMPTY`,
  `location_id=EMPTY`, `payments_url/query_url=EMPTY`,
  `supports_subscription_schedule=f`, `provider_api_key` length 64 (set).
  → **The HighLevel-facing association/config never populated local metadata:
    the local row is empty of provider metadata/location.**
  Unique: `uq_payment_provider_config_integration(integration_id)`; index on
  `location_id`. FK integration_id → integrations ON DELETE CASCADE.
- `oauth_tokens`: 1; `oauth_states`: 0; `webhook_subscriptions`: 0.
- No DB mutation performed. Migrations untouched.

## 4. v3 endpoint findings (authoritative — marketplace.gohighlevel.com v3 docs)
1. **Provider association (create integration)** — `POST /payments/custom-provider/provider`, header `Version: v3`.
   - `locationId` is a **QUERY** parameter (required), NOT in the body.
   - Body (all required): `name`, `description`, `paymentsUrl`, `queryUrl`,
     `imageUrl`, `supportsSubscriptionSchedule` (bool).
   - Response: `_id`, `name`, `description`, `paymentsUrl`, `queryUrl`, `imageUrl`,
     `locationId`, `marketplaceAppId`, `paymentProvider`, `deleted`, `createdAt`,
     `updatedAt`, `traceId`.
   - **This call registers the provider NAME/URLs/LOGO shown in HighLevel.**
2. **Provider configuration/connect** — `POST /payments/custom-provider/connect`, header `Version: v3`.
   - `locationId` is a **QUERY** parameter (required).
   - Body: `live:{apiKey,publishableKey}` and `test:{apiKey,publishableKey}`.
   - Response: same provider shape as create-integration.
   - This endpoint stores LIVE/TEST API keys — NOT provider metadata.
3. **Fetch config** — `GET /payments/custom-provider/connect`, `locationId` query param, `Version: v3`; returns provider shape.
4. **Disconnect config** — `DELETE /payments/custom-provider/connect`, `locationId` query param. (Separate sorry-typo: `Delete` integration endpoint also exists.)
5. **Token / user type** — `POST /oauth/token` (form-urlencoded). Request fields are
   **snake_case**: `client_id`, `client_secret`, `grant_type`
   (`authorization_code`|`refresh_token`|`client_credentials`), `code`,
   `refresh_token`, `user_type` (`Company`|`Location`), `redirect_uri`.
   Response is **camelCase**: `access_token`, `token_type`, `expires_in`,
   `refresh_token`, `scope`, `userType`, `locationId`, `companyId`,
   `approvedLocations`, `userId`, `planId`, `isBulkInstallation`,
   `installToFutureLocations`, `approveAllLocations`.
   - `locationId` present only for **Sub-Account (Location)** tokens; `Company`
     tokens carry `companyId` + `approvedLocations` + bulk flags.
5a. Auth URL `https://marketplace.gohighlevel.com/oauth/chooselocation` (current code
   matches); token URL `https://services.leadconnectorhq.com/oauth/token`.
6. **Required payment scopes**: `payments/orders.readonly`, `payments/orders.write`,
   `payments/subscriptions.readonly`, `payments/transactions.readonly`,
   `payments/custom-provider.readonly`, `payments/custom-provider.write`,
   `products.readonly`, `products/prices.readonly`. (Current provider uses `["read","write"]` — insufficient.)
7. **Agency vs location install**: `user_type=Location` → per-sub-account token with
   `locationId`; `user_type=Company` → agency token (bulk/`approvedLocations`).
   RVPay targets Sub-accounts, so `user_type=Location` is correct — but field name
   and Company-token handling are open questions.
8. **Existing association/config**: repeated create for same location errors; explicit
   delete/disconnect endpoints exist. Prefer fetch-exists → reuse (or delete→create)
   over a status-code “already exists” guess.
9. **Which response is provider metadata**: the **Create/Get integration**
   (`/payments/custom-provider/provider`) and **FetchConfig** (`GET .../connect`)
   both return the provider metadata shape; metadata is REGISTERED by the
   integration/association call. `connect` is only for live/test keys.

## 5. Current defects in `HighLevelPaymentProviderClient` (Agent 04 must fix)
- D1 `CreateProviderAssociation` (`POST /payments/custom-provider/provider`)
  sends `{"locationId":...}` in the BODY and NO provider metadata. v3 wants
  `locationId` in QUERY and metadata (`name,description,paymentsUrl,queryUrl,
  imageUrl,supports..`) in the BODY. Metadata never registered; likely 422.
- D2 `CreateProviderConfig` (`POST /payments/custom-provider/connect`) sends provider
  METADATA and puts `locationId` in the BODY. v3 wants `locationId` in QUERY and a
  BODY of `live`/`test` api-key objects. Keys never stored; likely 422.
- D3 `FetchProviderConfig` (`GET /payments/custom-provider/connect`): `locationId`
  query + camelCase parse — CORRECT. Keep.
- D4 `oauth/service.go RegisterProvider` treats `ErrBadRequest`/`ErrUnprocessableEntity`
  from BOTH association & connect as “already exists” and merely fetches. Conflates
  contract errors with duplicates. Replace with explicit fetch-confirm-or-(re)create.
- D5 OAuth token field casing: `providers/highlevel.go` sends camelCase
  (`clientId,clientSecret,grantType,redirectUri,userType`) for `POST /oauth/token`;
  v3 doc specifies snake_case (`client_id,client_secret,grant_type,redirect_uri,
  user_type`). Same in `RefreshToken`. Verify on a real install; likely switch to
  snake_case.
- D6 Scopes `["read","write"]` lack required `payments/*`,`products/*` scopes (§4.6).
- D7 Credential/code leakage via logs (violates rules):
  - `oauth/service.go` logs the OAuth `code` (~L194).
  - `oauth/service.go` logs `tokenResp.AccessToken` (~L399).
  - `providers/highlevel.go` logs full token-request `data` incl. `clientSecret` (~L156).
  - `cmd/grpc-service/main.go getPostgresConnectionURL` prints DB DSN (password).
- D8 Local `payment_provider_configs` row (dev tenant) is empty of provider
  metadata/location (name/urls/location empty; only api_key set). Root cause D1/D2;
  fixing + persisting fetched/returned config should repopulate it.
- D9 `providers.TokenResponse` has no `userType`/`companyId`/`approvedLocations`; a
  Company token would make the stateless callback return `ErrMissingLocationID`.
- D10 Re-install/duplicate: with delete/disconnect endpoints available, replace the
  “create→on 4xx fetch” heuristic with a deterministic exists?→fetch : create flow.

## Current correct behavior to PRESERVE (Agent 02)
- Single code exchange (stateless + state paths) via `processCallbackWithToken`;
  raw code never re-exchanged.
- Stateless Marketplace callback resolves HighLevel platform by slug (never creates
  platform); provisions tenant client + integration mapped by locationId.
- INSTALL webhook provisions tenant idempotently; config create idempotent.
- `locationId → integration` resolution used by webhook and payments services.
- Error surfaces sanitized (mostly); D7 removes the remaining leaks.

## Exact files Agent 04 should modify
- `clients/providers/highlevel_payment_provider.go` — fix association + connect
  payloads & `locationId` as query (D1/D2); keep fetch config; adapt struct.
- `clients/providers/payment_provider.go` — align `ProviderConfig`/signatures with
  v3 (metadata for provider association; keys for connect).
- `clients/providers/highlevel.go` — token request snake_case (D5), scopes (D6),
  user_type (D5/D7), remove clientSecret log (D7), optional userType/companyId parse.
- `clients/oauth/service.go` — RegisterProvider idempotency (D4), remove code/access
  token log lines (D7).
- `clients/cmd/grpc-service/main.go` — remove DB-DSN print (D7) only if touched.
- Tests: `clients/providers/highlevel_payment_provider_test.go`,
  `clients/providers/highlevel_test.go`, `clients/oauth/service_test.go`.
- `clients/db/query/payment_provider_configs.sql` (+sqlc regen) ONLY IF the local
  config must store live/test keys or new fields — verify schema first, prefer
  existing columns.

## Files/directories to AVOID in Agent 04
- `transactions/`, `deposits/`, legacy `integrations/`, pawaPay code,
  `shared/`, `grpc/go/`, `third_party/`, `protobuf/`, `infra/`, `deploy/`,
  `nginx/`, `.env`, `.git/`.
- `clients/health/`; non-HighLevel providers.
- `db/migrations/*` unless the existing schema cannot support the fix (prefer
  existing columns).
- `db/sqlc/*` and `grpc/go/*` — regenerate if needed; never hand-edit.
- `clients/payments/*` unless a payment contract changes (not part of D1-D10).

## Required tests (add/update with code in Agent 04)
- Association payload: body has `name,description,paymentsUrl,queryUrl,imageUrl,
  supportsSubscriptionSchedule`; `locationId` in QUERY; exact method + path.
- Connect payload: body has `live`/`test` {apiKey,publishableKey}; `locationId` in QUERY.
- Fetch-config unchanged (locationId query, camelCase parse).
- Token request: `POST /oauth/token` form snake_case keys + `user_type`;
  scopes set; single exchange preserved.
- Stateless Marketplace callback: tenant provisioning + single code exchange +
  corrected RegisterProvider; best-effort non-fatal preserved.
- No credential/code substrings in returned errors (assert absent).
- Existing clients suite green: `go test ./... -count=1`, `go vet`, `gofmt`.

## Marketplace dashboard prerequisites (record; NOT implemented by Agent 04 code)
- App scopes = payment list (§4.6) set in marketplace app.
- Redirect URL = `HIGHLEVEL_REDIRECT_URI`; Webhook URL = GHL webhook endpoint;
  SSO key; custom page URLs (`paymentsUrl`/`queryUrl`).
- Provider profile: name/description/logo; type = OneTime; category = Third-party provider.
- Connect contract expects `live`/`test` apiKey + publishableKey — Agent 04 must
  decide who supplies RVPay keys. Record as a checklist in the checkpoint.

## Risks / open questions
- Token field casing (snake vs camel): v3 doc = snake_case; current code =
  camelCase. Confirm on a real install before rewriting.
- Live/test key ownership: who issues RVPay publishable/api keys for `connect`?
- Company token: if an agency/company install occurs, `locationId` is absent —
  define behavior (fail clearly vs bulk/`approvedLocations`).
- Scope broadening may require GHL marketplace re-approval/verification.
- Local config empty row: fix must reconcile local `payment_provider_configs`
  with the real HighLevel association.
- Platform row `highlevel` assumed present; if missing, fail clearly (never create).
- Dev DB rows are dev/test artifacts (tenant teXi7…) — not production data.

## Status
- [x] Read-only audit complete. Deliverables: `.clineignore.md`, `.clinerules.md`,
  `.clinecheck.md`, `.service-checkpoint.md` (Agent 03 Baseline appended).
- [ ] Agent 04 implementation (not started — do not begin until instructed).
